# Baseline Selection Packet

- Project/state/snapshot version:
- Direction and frozen S01 scope:
- Why a primary baseline is required before S02:
- User-supplied baseline, if any:
- Active `BL-` selection, if any:

| BL ID | Model/variant | Task/data/metric fit | Resource fit | Provenance status | Known limitation | Selection status |
|---|---|---|---|---|---|---|

## Required Human Decision

- Select one `BL-` ID, provide a different baseline, or request scope reframe:
- Rationale/constraint:
- Explicit G1 outcome (`APPROVED`, `HOLD`, or `REVISE`):
- Consequence and next state:

## Integrity Notes

- This selection fixes a comparison contract; it is not novelty evidence.
- Broad prior-art search continues beyond the selected model family.
- A later baseline/configuration change re-enters S01 and invalidates dependent work.
